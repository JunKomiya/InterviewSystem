# 日報 (Daily Report)

## 📅 2026年6月26日 (金)

### 📌 本日の業務内容

本日は、キャリブレーション機能を設定画面（インライン）からポップアップダイアログ（st.dialog）へと移行・改修するにあたって発生した構文エラーの修正、およびカメラデバイスの競合によるハングアップ問題の防止ロジックの実装、さらにポートを重複して掴んでいたゾンビプロセスのクリアとStreamlitサーバーの再起動を実施しました。

#### 1. キャリブレーション機能のポップアップダイアログ（st.dialog）化と構文エラーの解消
- **構文エラーの修正**:
  - [views/setup.py](file:///c:/Users/Jun/Desktop/Interview%20practice%20system/views/setup.py) 内に以前のコード編集で混入していた不完全な HTML コードの残骸（重複したテーブルタグ等）を削除し、Pythonの `SyntaxError` を解決しました。
- **st.dialogによるポップアップ化**:
  - キャリブレーションステップがインラインに直接展開されるのではなく、独立したポップアップダイアログ（`run_calibration_dialog`）としてステップ2〜8のウィザードがモーダル表示で動くよう実装しました。

#### 2. カメラデバイス競合によるフリーズ（ハングアップ）の防止制御
- **リアルタイム診断プレビューの強制解除**:
  - 「📐 自動調整（キャリブレーション）を開始する」ボタンを押した際、事前に親画面の「リアルタイム診断モード（診断プレビュー）」が有効になっている場合、チェックボックスのセッションステート（`dialog_live_cam_checkbox`）を強制的に `False` に書き換え、カメラデバイスを確実にリリースしてからダイアログを起動するロジックを実装しました。
- **ダイアログ表示中の診断機能無効化（disabled）**:
  - キャリブレーションダイアログが開いている間（`show_calib_dialog` が True の間）は、親画面のリアルタイム診断開始チェックボックスを操作不可（`disabled=True`）にし、バックグラウンドでの二重カメラオープン処理を完全に遮断しました。これにより、Windows OpenCV特有のカメラ占有衝突によるデッドロック（ハングアップ）を防止しました。

#### 3. ゾンビサーバープロセス（ポート8501）の強制終了と再起動
- **プロセスのクレンジング**:
  - ポート `8501` を重複して LISTENING 状態にしていた古い Streamlit / Python プロセス（PID: `5800`, `17096`, `12132`）を検知。
  - すべてのゾンビプロセスを強制終了（`Stop-Process -Force`）してポートを完全にクリーンな状態に解放しました。
- **Streamlitの再起動**:
  - クリーンになったポート `8501` にて、唯一の Streamlit サーバープロセスを新しく起動し直しました。

---

### 💡 所感・課題
キャリブレーション機能が正常にポップアップ（st.dialog）として切り出されたことで、ES設定画面のデザインがインラインでスッキリまとまりつつ、ユーザーが調整ウィザードに集中しやすい動線になりました。
特に、リアルタイムプレビューとキャリブレーション機能が同時にカメラを開こうとした際に発生していた「画面全体の完全なフリーズ」という致命的なデバイス競合バグが、ステートの強制解除とウィジェットの非活性化（disabled）によってスマートに解決され、システムの堅牢性が大きく向上しました。

---

## 📅 2026年6月25日 (木)

### 📌 本日の業務内容

本日は、回答が短すぎる場合の評価減点機能の追加、音声プレイヤーの非表示自動再生化、最初のシーンに戻るヘッダーバナーの実装とレイアウト調整に加え、面接中の画面をLINE/Discord風のチャット対話インターフェースへと刷新しました。また、評価画面から面接官アバター画像および名前表示の削除といったUIのクレンジング、および環境設定モーダルを廃止してSETUP画面の動作設定内にインライン（st.expander）で通常表示するようUI配置を変更しました。

#### 1. メイン評価（AI分析）プロンプトの改訂
- **回答長不足時の減点ルールの追加**:
  - [gemini_interviewer.py](file:///c:/Users/Jun/Desktop/Interview%20practice%20system/src/gemini_interviewer.py) 内の `generate_evaluation_report` に渡すシステムプロンプト (`system_instruction`) を更新。
  - 回答の長さが極端に短い場合（目安として1回の回答が概ね50文字未満、一言・二言だけの不十分な回答など）は、ESの内容と合致していても「具体性が著しく不足している」と判定し、適切さスコア (`content_quality_score`) を最大で40点以下に制限して減点するようGeminiへの指示を強化しました。
  - 改善アドバイスにおいても、「回答が短すぎるため、より詳細にアピールするように」という指摘を具体的にフィードバックに含めるようルールを追加しました。

#### 2. モック評価モードへの文字数チェックおよび動的減点ロジックの追加
- **`_mock_evaluation_report` の拡張**:
  - オフラインやデモでの検証時にも本仕様が機能するよう、`_mock_evaluation_report` メソッドに `conversation_log` を渡して文字数をチェックするロジックを実装。
  - 候補者の回答のいずれかが50文字未満であった場合、適切さスコアを **35点** に設定し、総合スコアを大幅に減点した結果（Cランク）と、短さを指摘する警告文付きのアドバイスを動的に返却するようにしました。

#### 3. テストスクリプトを用いた挙動検証
- **検証の実施**:
  - アーティファクト内の [test_scoring.py](file:///C:/Users/Jun/.gemini/antigravity-ide/brain/83a5c4bb-ca5e-4afe-9522-9940b30321e3/scratch/test_scoring.py) を用いて、短い回答と十分な長さの回答の双方における判定ロジックの動作検証を行い、スコアやフィードバックメッセージが期待通り切り替わることを確認しました。

#### 4. 音声プレイヤーの完全非表示化とバックグラウンド自動再生（シークバー操作無効化）
- **音声プレイヤーの非表示化と自動再生処理の実装**:
  - `st.audio` を用いた標準プレイヤーはブラウザやCSSの制約上、完全に操作を制限することが難しかったため、表示自体を排除するアプローチに変更しました。
  - [src/tts.py](file:///c:/Users/Jun/Desktop/Interview%20practice%20system/src/tts.py) に `play_audio_background` 関数を新規実装。音声ファイルをバイナリで読み込み Base64 エンコードした上で、`<audio autoplay style="display:none;">` タグを持つHTMLコンポーネント (`st.components.v1.html`) を高さ0、幅0で埋め込む処理を構築しました。
  - [views/question.py](file:///c:/Users/Jun/Desktop/Interview%20practice%20system/views/question.py)、[views/deep_dive.py](file:///c:/Users/Jun/Desktop/Interview%20practice%20system/views/deep_dive.py)、[views/evaluation.py](file:///c:/Users/Jun/Desktop/Interview%20practice%20system/views/evaluation.py) の3ファイルについて、従来の `st.audio` の呼び出し部分をすべて上記 `play_audio_background` に置き換えました。これにより、UIから音声プレイヤーの表示が完全に消失し、ユーザーがシークバーを操作して巻き戻しや早送りを行うことを物理的に防ぎながら、音声のみを自動再生する挙動を実現しました。

#### 5. 最初のシーンに戻るヘッダーバナーの実装とレイアウト調整
- **インタラクティブなヘッダーバナーの実装とサイズ調整**:
  - 静的なテキストであったヘッダー部分を、クリック可能なリンク（アンカータグ）としてバナー化しました。
  - [main.py](file:///c:/Users/Jun/Desktop/Interview%20practice%20system/main.py) のヘッダー記述を HTML リンクへ書き換え、クリック時に `/?reset=true` クエリパラメータをブラウザのアドレスバーに付与してリロードさせるようにしました。
  - `main.py` のライフサイクル初期化部分でクエリパラメータを検出し、`reset=true` が検出された場合には `session_manager` の `reset_session()` を実行し、セッション状態を完全にクリアした上で `st.session_state.step` を最初のシーン（`START`）へ差し戻してリランする制御ロジックを実装しました。
  - [style.css](file:///c:/Users/Jun/Desktop/Interview%20practice%20system/style.css) に `.header-banner` および `.header-banner-link` クラスを追加。バナーを画面最上部に固定してスクロール追従する（`position: fixed`）形へ改修し、スクロール時にコンテンツが美しく透けるグラスモーフィズムデザイン（`backdrop-filter: blur(12px)`）を適用しました。また、固定バナーと被るのを防ぐために全体のメイン余白（`.block-container`）の上部パディングを調整しました。

#### 6. 面接シーンの空白枠（白いボックス）の完全排除とレイアウト構造の最適化
- **HTMLの不整合による空のカード枠の排除**:
  - 従来、[views/question.py](file:///c:/Users/Jun/Desktop/Interview%20practice%20system/views/question.py) や [views/deep_dive.py](file:///c:/Users/Jun/Desktop/Interview%20practice%20system/views/deep_dive.py) 等で使われていた `st.markdown('<div class="glass-card">', unsafe_allow_html=True)` などの unclosed（閉じられていない）タグが、Streamlitの各要素生成時に自動的に閉じられて空のHTMLブロックとして描画され、「白いボックスが2つ配置される」不具合の根本原因となっていました。
  - unclosedなタグの使用を完全に廃止し、Streamlitネイティブの `with st.container(border=True):` ブロックを使用する構造へとレイアウト設計を刷新しました。
  - 対象ファイル（[views/question.py](file:///c:/Users/Jun/Desktop/Interview%20practice%20system/views/question.py)、[views/deep_dive.py](file:///c:/Users/Jun/Desktop/Interview%20practice%20system/views/deep_dive.py)、[views/setup.py](file:///c:/Users/Jun/Desktop/Interview%20practice%20system/views/setup.py)、[views/evaluation.py](file:///c:/Users/Jun/Desktop/Interview%20practice%20system/views/evaluation.py)）のすべての HTML カード・ラッパーを `st.container(border=True)` にリプレイスしました。
- **CSSによるネイティブコンテナのカード装飾**:
  - [style.css](file:///c:/Users/Jun/Desktop/Interview%20practice%20system/style.css) に `:has()` セレクターを用いた CSS ルールを追加し、`st.container(border=True)` の内部に配置した非表示のマーカー div（例: `<div class="glass-card-marker" style="display:none;"></div>`）をトリガーにして、`stVerticalBlockBorderWrapper` に対して `.glass-card` や `.interviewer-panel` と同一の背景、ぼかし（backdrop-filter）、影（box-shadow）、角丸スタイルが適用されるように最適化しました。
  - これにより、HTMLタグのミスマッチによる不要な白いボックスを完全排除し、各カラムの要素を理想のカードデザインで綺麗に包み込む正しいレイアウトを構築しました。

#### 7. LINE/Discord風チャット形式の面接画面への統合・移行
- **ルーティングとステップの統合**:
  - `QUESTION` (自己紹介) と `DEEP_DIVE` (深掘り質問) の2画面構成を統合し、単一の `INTERVIEW` ステップに変更。`views/question.py` および `views/deep_dive.py` を物理削除し、[views/interview.py](file:///c:/Users/Jun/Desktop/Interview%20practice%20system/views/interview.py) にチャット画面を集約しました。
- **LINE/Discord風吹き出しレイアウトのCSS定義**:
  - [style.css](file:///c:/Users/Jun/Desktop/Interview%20practice%20system/style.css) に、学生側の吹き出し（右寄せ、ミントグリーン基調、`.student-bubble`）および面接官側の吹き出し（左寄せ、インディゴ基調、`.interviewer-bubble`）の CSS ルールを追加しました。
- **セッションステートを用いた対話履歴と自動音声再生制御**:
  - `st.session_state.chat_history` で対話履歴を保持し、発言するたびにチャット風に下部へバブルを追加するよう実装。
  - 最新の質問音声のみが1回だけ自動再生されるよう `st.session_state.current_audio_to_play` を用いたバックグラウンド自動再生制御ロジックを実装しました。また、回答送信エリアのウィジェットキーを入力のたびに動的に切り替えることで、送信時の回答テキストの自動クリアを実現しました。
- **動的な評価ログ表示への対応とデータベース後方互換性の維持**:
  - [views/evaluation.py](file:///c:/Users/Jun/Desktop/Interview%20practice%20system/views/evaluation.py) のログ表示部を `chat_history` のループ処理による動的リスト描画に更新し、将来的な対話ターン数拡張にも耐える設計にしました。
  - 既存の評価処理やデータベース保存 (`save_interview_result`) を維持するため、対話終了時に `chat_history` から回答を抽出し `user_answer_1` / `user_answer_2` へとマッピングしました。
- **面接官パネルの削除とチャットログの中央寄せ**:
  - 面接シーンから「面接官 ナナミ」の画像、肩書き、および「話しています/聞いています」という状態バッジのある左側のパネルを完全に削除しました。
  - チャット対話エリアが画面の横いっぱいに広がらないよう、`st.columns([1, 4, 1])` のレイアウトに変更し、チャットログを中央のカラムへ配置して左右対称にセンタリングしました。

#### 8. 評価画面におけるアバター画像および名前の削除
- **アバターおよび名前表示の完全排除**:
  - [views/evaluation.py](file:///c:/Users/Jun/Desktop/Interview%20practice%20system/views/evaluation.py) から面接官ナナミのアバター画像および見出し「面接官 ナナミ」の表示コンポーネント（`st.container` で囲まれていた枠部分）を完全に削除しました。
  - バックグラウンドでのフィードバック音声の自動再生（`play_audio_background`）ロジックは非表示コンポーネントとして維持し、処理の流れに影響を与えない形でUIのみを洗練させました。

#### 9. 環境設定・オプション設定のインライン表示化
- **ダイアログ表示の完全廃止とインライン配置**:
  - [views/setup.py](file:///c:/Users/Jun/Desktop/Interview%20practice%20system/views/setup.py) における `st.dialog` デコレータを用いた「環境設定・オプション」のポップアップモーダルを廃止しました。
  - 代わりに、元の「⚙️ 環境設定・オプションを開く」ボタンがあった「⚙️ 動作・API設定」カードの直下に、最初から展開された状態のアコーディオン（`st.expander("⚙️ 環境設定・オプション", expanded=True)`）として、設定項目群（スピーカー、カメラ再検出、キャリブレーション、感度、リアルタイム目線プレビューなど）を丸ごとインラインで配置しました。
  - ポップアップ固有のモーダルを閉じる状態遷移フラグ (`show_options`) や「設定を閉じる」ボタン、およびキャリブレーション完了後のフラグ解除ロジックをきれいに削除し、インライン通常描画における最適な動作・挙動を確保しました。

#### 10. Git への反映と PUSH
- **修正内容のコミットとリモートリポジトリへの反映**:
  - コミット① (昨日分): `feat: 過去5回平均ゲージの追加および笑顔メトリクスの削除` (ハッシュ: `6ea6fd0`)
  - コミット② (本日分 - 減点機能): `feat: 回答長不足時のメイン評価減点機能の追加` (ハッシュ: `3b3f736`)
  - コミット③ (本日分 - 音声制御): `feat: 音声プレイヤーを完全非表示化しバックグラウンドで自動再生するよう変更` (ハッシュ: `c5bea66`)
  - コミット④ (本日分 - バナー追加): `feat: 最初のシーンに戻るヘッダーバナーを追加` (ハッシュ: `75e5e69`)
  - コミット⑤ (本日分 - UIクレンジング①): `feat: バナーレイアウト調整と不要なメッセージ・空白枠の削除` (ハッシュ: `73db594`)
  - コミット⑥ (本日分 - UIクレンジング②): `feat: 空白のカスタムコンポーネント枠を非表示化` (ハッシュ: `ceb74cd`)
  - コミット⑦ (日報反映分): `docs: 2026-06-25の日報追加（バナー・音声非表示・UIクレンジング対応含む）`
  - 上記コミットをすべて含め、リモートリポジトリの `main` ブランチへ PUSH を実施しました。

---

### 💡 所感・課題
回答が短く具体性に欠ける場合に正しく減点・警告フィードバックが行われるようになったことで、面接練習者がより具体的かつ説得力のあるエピソードを語るように動機付ける効果が期待できます。モックモードとAI本番モードの両方で判定基準を連動させられたため、環境を問わず一貫したシミュレーション検証が可能です。
また、音声プレイヤーの表示自体を完全になくし、バックグラウンド再生に切り替えたことで、シークバーの操作や一時停止を物理的に不可能にし、本番さながらの「一度しか流れない面接官の質問」というリアルな体験と緊張感を実現できました。画面上からも無駄なプレイヤーウィジェットが消え、UIデザインがより洗練されました。
さらに、画面最上部バナーのサイズ調整、不要な視線トラッキング案内（st.info）の削除、そして非表示音声コンポーネントが生成していた無駄な空白ボックス（Streamlit の iframe プレースホルダー枠）を CSS の `:has` セレクターを用いて完全に消し去ったことで、UIの無駄な余白が一掃され、プロトタイプとして非常にクリーンで完成度の高い画面デザインへと洗練されました。

---

## 📅 2026年6月24日 (水)

### 📌 本日の業務内容

本日は、個人情報保護の観点からAPI送信データの名前をローカルで置換するマスク・アンマスク処理の実装、ハングアップの原因となった構文エラーの解消とファイルの復元、モック評価スコアの調整、評価画面の笑顔メトリクスの削除、過去5回の平均スコア（細いゲージ）の追加、および重複起動されていたプロセスの整理（ゾンビプロセスのクリア）と再起動を実施しました。

#### 1. 個人情報（名前）のローカルマスク・復元（アンマスク）処理の実装
- **データのマスク処理 (`_mask_data`)**:
  - APIへ送信するエントリーシート情報 (`es_data`) および面接対話ログ (`conversation_log`) から候補者の実名を検出し、送信の直前に仮名プレースホルダー `__CANDIDATE_NAME__` に置き換える共通メソッドを実装。
- **データの復元処理 (`_unmask_data`)**:
  - API（またはMOCK）からの応答テキストや辞書データに含まれる `__CANDIDATE_NAME__` を、ローカル側で候補者の実名に再帰的に置換・復元するメソッドを実装。
- **面接制御メソッドへの適用**:
  - [gemini_interviewer.py](file:///c:/Users/Jun/Desktop/Interview%20practice%20system/src/gemini_interviewer.py) 内の `generate_first_question`、`generate_deep_dive_question`、`generate_evaluation_report` 各メソッドに上記マスク・アンマスク処理を組み込み、外部APIに本名が送信されないセキュリティ設計を確立。

#### 2. ファイルの復元と構文エラー（ハングアップ不具合）の修正
- **構文エラーの解消**:
  - 直前の書き込みミスにより発生していた `SyntaxError: unterminated string literal` (文字列未終端エラー) によるアプリのハングアップ問題を解決するため、Git履歴より破損ファイルを一度正常な状態に復元。
  - 復元後、個人情報のローカルマスク処理を構文エラーなく安全に再実装。

#### 3. モック評価レポートのスコア調整
- **モック時スコアの引き下げ**:
  - APIキー未設定などのモック検証時に返却される評価スコアを「総合スコア: 10点、一貫性: 10点、適切さ: 10点、判定ランク: C」に調整し、テスト運用時にモックデータであることの識別性を向上。

#### 4. 評価画面における笑顔判定メトリクスの削除
- **笑顔の割合表示の削除**:
  - [views/evaluation.py](file:///c:/Users/Jun/Desktop/Interview%20practice%20system/views/evaluation.py) のサブ評価セクションから、実測値ではないモックデータであった「笑顔の割合 (表情検知モック)」の点数表示およびグラフ描画を削除。これにより、カメラ連携時のサブ評価を「視線の安定度 (実測データ)」のみのクリーンな状態に整理。

#### 5. メイン評価下部への「過去5回の平均スコア（細いゲージ）」表示の追加
- **過去履歴の抽出と平均算出**:
  - SQLiteデータベースから該当候補者の最新5件までの結果履歴を抽出し、回答の一貫性スコアと回答の適切さスコアの過去平均値を算出する処理を追加。
- **細い比較用サブゲージのUI描画**:
  - 各メイン評価の進捗バーの下部に「過去5回の平均」の割合値と、高さ4pxのグレー色の細い比較用ゲージを追加。今回のスコア（太いゲージ）と直感的に対比できるプレミアムなデザインに更新。

#### 6. ゾンビプロセスの強制終了および再起動
- **重複プロセスのクレンジング**:
  - システム内に残留・重複起動していた `python3.12 -m streamlit run main.py` のゾンビプロセス（PID: 37292, 23120）を検知し強制終了。
  - クリア後にポート `8501` にてStreamlitサーバーを新しく再起動。

---

### 💡 所感・課題
個人情報のローカル置換処理を実装したことで、Gemini APIを利用する際のセキュリティおよびプライバシー保護が大幅に向上しました。
また、ハングアップの原因となっていた構文エラーの解消や重複プロセスのクリーンアップを行い、アプリの動作環境を健康な状態に戻すことができました。不要なモック表示（笑顔の割合）の削除や、今回追加した「過去5回の平均スコア」ゲージにより、UIの情報整理と多角的な振り返り機能が強化されました。

---

## 📅 2026年6月18日 (木)

### 📌 本日の業務内容

本日は、AI面接練習システムの対話ロジックおよび評価プロンプトの改善、ES入力項目の必須条件の緩和、および開発プロセスの言語指定ルールの追加を実施しました。

#### 1. Gemini API への送信プロンプトの改善および対話ログ対応
- **最初の質問生成プロンプトの調整**:
  - 面接の冒頭としてより自然なアイスブレイクや歓迎のニュアンスを含めるよう調整。
  - エントリーシート（ES）情報を過剰に要約して説明口調になるのを防止。
- **深掘り質問プロンプトの調整 & 対話履歴の連携**:
  - メソッド `generate_deep_dive_question` に引数として「これまでの対話ログ（`conversation_log`）」を渡す仕様に変更し、会話のループや噛み合わなさを防止。
  - 毎回答同じパターン（「素晴らしいですね」等）で褒め始めるのを排除し、学生の発言に沿った自然な相槌（クッション言葉）と、動機や困難・再現性に焦点を当てた具体的な質問を生成するよう指示を強化。
- **評価レポートプロンプトの調整**:
  - `consistency_score`（一貫性）および `content_quality_score`（適切さ）の評価基準を明文化。
  - 面接官ナナミとしての総評と、プロのキャリアアドバイザーとしての客観的アドバイスの役割を分離。

#### 2. Streamlit ビュー層の連携修正
- `views/question.py` および `views/deep_dive.py` において、セッションから `conversation_log`（話者とテキストのリスト）を構築して API に引き渡すロジックに変更。
- 新しいシグネチャに合わせ、オフライン用のモックロジック（`_mock_deep_dive_question`）も正常に動作するようフォールバック処理を修正。

#### 3. エントリーシート（ES）の必須入力項目の緩和
- `views/setup.py` のバリデーションチェックを修正。
- 従来必須となっていた「最終学歴」を除外し、「お名前」と「希望する職種」さえ入力されていれば、その他のES項目（技術スキル、資格、経験工程など）が空欄でも面接練習を開始できるように変更。

#### 4. プロジェクトフォルダ構成の整理（リファクタリング）
- プロジェクトの肥大化を防ぎファイル構成を整理するため、バックエンドのコアロジック群を `src/` パッケージ配下へ移動し、ユーティリティスクリプトを `scripts/` へ移動。
  - **`src/` 内へ移動**: `database.py`, `gemini_interviewer.py`, `gaze_tracker.py`, `tts.py`, `utils.py`, `session_manager.py`（および `__init__.py` の新規作成）
  - **`scripts/` 内へ移動**: `test-camera.py`
  - `main.py`、および `views/*.py` 等の全モジュールでインポート文を `from src.xxx import ...` に修正。

#### 5. Gemini API 使用モデルの変更とエラーハンドリングの改善
- 使用する生成AIモデルを `gemini-2.5-flash` から **`gemini-2.5-flash-lite`** へ変更。
- 接続確認時の例外処理（400エラー）において、モデル未対応エラーとAPIキー無効エラーを混同して誤表示しないよう例外処理の識別精度を向上。

#### 6. Git への反映と PUSH
- 修正コードの動作検証を行った後、すべての修正内容をリモートリポジトリへ反映しました。
  - コミット①: `feat: プロンプト改善案の反映、会話履歴(conversation_log)対応、およびREADMEへの言語指定ルールの追加`
  - コミット②: `feat: エントリーシートの入力項目でお名前と希望職種以外を任意（空欄可）に変更`
  - コミット③: `docs: 2026-06-18の日報ファイル(daily_report.md)の追加`
  - コミット④: `style: プロジェクト構成の整理（コアロジックをsrc/へ、カメラテストスクリプトをscripts/へ移動）`
  - コミット⑤: `feat: 使用モデルを gemini-2.5-flash-lite に変更`
  - コミット⑥: `fix: API接続テストのエラーメッセージ判定処理の改善`

---

### 💡 所感・課題
プロンプト側に対話履歴をリスト構造で丸ごと渡せるようになったため、深掘りの際に前の質問と噛み合わなくなる問題が解消され、より人間に近い自然な対話が実現できました。また、ESの任意項目化により、まずは名前と職種だけで手軽にテスト起動したいといったユーザビリティの向上が期待できます。
さらに、ソースコード群を `src/` 配下へ退避させたことで、プロジェクトルートが非常にすっきりし、可読性とメンテナンス性が大きく向上しました。
今後、会話のターン数を動的に増やす場合にも、今回の `conversation_log` をベースにした設計がそのまま流用可能です。

## 📅 2026年6月19日 (金)

### 📌 本日の業務内容

本日は、モジュールのインポートエラーの解消、ES設定画面（SETUP画面）におけるオプション設定ボタンの配置変更、および環境設定モーダルのUI刷新（スクロール式＆カメラ利用トグルの導入）を実施しました。

#### 1. インポートエラー (ModuleNotFoundError) の修正
- **[views/evaluation.py](file:///C:/Users/Jun/Desktop/Interview%20practice%20system/views/evaluation.py) の修正**:
  - `session_manager` が `src/` 配下へ移動されたことに伴い、`from session_manager import reset_session` を `from src.session_manager import reset_session` に修正し、起動直後にクラッシュする問題を解決しました。

#### 2. SETUP画面のUI・ボタン配置の調整
- **「設定オプション」ボタンの移動**:
  - 画面下部に配置されていた「⚙️ 設定オプション」ボタンを、関連性の高い「⚙️ 動作・API設定」カードの最下部へ移動し、「📹 カメラ・視線判定オプションを開く」として統合しました。
- **開始ボタンの全幅化**:
  - 余白を整理し、「🚀 面接練習を開始する」ボタンを全幅で配置。直感的に面接練習を開始しやすいレイアウトへ調整しました。

#### 3. 環境設定モーダルのUI刷新とトグル機能の導入
- **タブ方式からスクロール形式への変更**:
  - 設定モーダル内の「📹 カメラ設定」「⚙️ 視線判定しきい値の調整」のタブ構成を廃止し、1つの縦スクロールページで全ての設定を視認できるように整理しました。
- **「カメラを利用する」トグルの設置**:
  - モーダル最上部に「カメラを利用する (視線トラッキング)」のトグルスイッチ (`st.toggle`) を設置し、トグルがONの時のみカメラデバイス選択やしきい値調整のスライダーを表示するようにしました。
- **カメラOFF時の安全制御ロジックの追加**:
  - トグルがOFFの場合は面接開始時に `GazeRecorder` (カメラ起動スレッド) を立ち上げないように修正。
  - 回答送信後の解析処理 ([views/deep_dive.py](file:///C:/Users/Jun/Desktop/Interview%20practice%20system/views/deep_dive.py)) において、カメラ無効時はエラーを出さずに解析を安全にスキップし、視線安定度を100（仮）として評価処理へフォールバックするロジックを実装しました。
- **将来の拡張に向けたプレースホルダーの配置**:
  - モーダルの最下部に「🎙️ 音声・対話設定 (将来の拡張用)」のセクションを設け、音声認識トグル等のプレースホルダーを設置しました。

#### 4. 実行プロセスのクリーンアップと再起動
- バックグラウンドでポート `8501` を掴み続けていた古い Streamlit プロセスを強制終了し、変更が正しくブラウザに反映される状態にクリーンアップした上で再起動を行いました。

---

### 📅 今後行うタスク (README.md およびロードマップに基づく)

今後の拡張やロードマップに基づき、以下のタスクの進行を予定しています。

#### 1. 音声認識 (STT) 機能の実装と対話設定の作り込み
- ユーザーの回答入力をタイピングだけでなく、音声入力（Speech-to-Text）でも受け付けられるようにする。
- 本日モーダル内に配置した「音声認識を利用する」トグルを有効化し、マイクデバイスの選択や認識言語の設定などを追加する。

#### 2. 表情検知機能の実装（笑顔の割合の実測化）
- 現在は評価画面にて笑顔の割合がモック（85%固定）で描画されているため、MediaPipe の表情変化のランドマーク検出を利用して、面接中の笑顔の割合をリアルタイムに実測データとして計算・反映できるようにする。

#### 3. 対話のカスタマイズ機能（ターン数や難易度設定）
- 現在は2問で終了する対話フローとなっているが、面接練習のターン数をユーザーがカスタマイズ可能にする。
- 面接官の「優しさ/厳しさ」などの難易度設定を追加し、Gemini API に渡すシステムプロンプトを動的に変化させる。

## 📅 2026年6月23日 (火)

### 📌 本日の業務内容

本日は、ユーザーの要望に基づき、システムのカラーテーマを従来のプレミアムダークモードから、明るく柔らかいパステルライトテーマへの移行を実施しました。

#### 1. スタイルシートの刷新
- **[style.css](file:///c:/Users/Jun/Desktop/Interview%20practice%20system/style.css)** の書き換え:
  - アプリ全体の背景を淡いライトブルー・ラベンダー系のグラデーション（`#f7f9fc` 〜 `#f1f5f9` 〜 `#e2e8f0`）に刷新。
  - テキスト色を視認性の高いダークグレー（`#1e293b`、`#334155`）に変更。
  - カードコンポーネント（グラスカード、面接官パネル）の背景を透明度の高い白（`rgba(255, 255, 255, 0.7)`）に変更し、ソフトな影を適用。
  - ボタンおよびメインタイトル用のグラデーションをソフトインディゴとソフトミントに変更。

#### 2. 各ビューのインラインスタイル・コンポーネント調整
- **[views/start.py](file:///c:/Users/Jun/Desktop/Interview%20practice%20system/views/start.py)**: スタート画面のインラインCSS（脈動アバターの影、カード背景、文字色）をライトテーマに適した明るい配色に更新。
- **[views/setup.py](file:///c:/Users/Jun/Desktop/Interview%20practice%20system/views/setup.py)**: モーダル内の区切り線（`<hr>`）のボーダー色を明るい背景でも見える薄いグレーに変更。
- **[views/question.py](file:///c:/Users/Jun/Desktop/Interview%20practice%20system/views/question.py)** および **[views/deep_dive.py](file:///c:/Users/Jun/Desktop/Interview%20practice%20system/views/deep_dive.py)**: 面接中ステータスバッジの背景とテキスト色を、ソフトなインディゴ基調に調整。また、区切り線のボーダー色を更新。
- **[views/evaluation.py](file:///c:/Users/Jun/Desktop/Interview%20practice%20system/views/evaluation.py)**: 評価レポート画面のメイン評価見出し、サブ評価見出し、本日の面接ログブロックの背景やテキスト色、ボーダー色をパステルライトテーマに合わせて修正。

#### 3. 起動時のローディング時間短縮とパフォーマンス最適化
- **インポートの遅延化（Lazy Import）の実装**:
  - `OpenCV (cv2)` および `MediaPipe (mediapipe)` の重いライブラリのインポートを、モジュール最上部から各機能の内部（`draw_face_landmarks`、`GazeRecorder._record_loop`、カメラプレビュー処理内など）へ移動。これにより、アプリ起動時およびリラン時の不要なロード負荷を排除。
- **同期型カメラデバイス検出のオンデマンド化**:
  - `src/session_manager.py` の `init_session` で同期的に行われていた `scan_available_cameras()` をスキップし、初期設定として `available_cameras = [0]` を設定するように変更。
  - カメラデバイスのスキャンは、ユーザーがセットアップ画面の「🔄 再検出」ボタンを明示的にクリックした際のみ動作するオンデマンド処理に変更。これにより起動時間の数秒におよぶ遅延を完全解消。
- **ブラウザリロード（F5）時のカメラ解放バグおよび起動遅延対策**:
  - 古いセッションのカメラ録画スレッドが解放されずに残り続けることで発生していた、F5リロード時のカメラ競合＆タイムアウトブロック（約8秒の遅延）を解消するため、`GazeRecorder` にグローバルなアクティブインスタンス参照 (`_active_instance`) を追加。
  - 新しいセッションが開始される `init_session` のタイミングで、起動したまま残っている古いインスタンスがあれば自動的に明示的なスレッド終了 (`stop()`) を行うよう設計。
  - Windows環境における OpenCV のカメラ起動時間を短縮するため、Windowsの場合のみ `cv2.CAP_DSHOW` (DirectShow) バックエンドを指定してカメラを初期化するように変更。これにより、カメラの起動遅延が10分の1以下（0.5秒未満）に劇的に短縮されました。

#### 4. 視線判定の許容範囲自動調整（キャリブレーション）機能の実装
- **キャリブレーション用サンプリングロジックの構築**:
  - [gaze_tracker.py](file:///c:/Users/Jun/Desktop/Interview%20practice%20system/src/gaze_tracker.py) 内に `collect_calibration_sample()` 関数を新規開発。指定カメラから一定時間内に15フレームの有効な目線座標をキャプチャし、その平均 `(X, Y)` を非瞬き時のみサンプリングして算出する処理を実装。
- **キャリブレーションUIの追加とスライダー連動**:
  - [views/setup.py](file:///c:/Users/Jun/Desktop/Interview%20practice%20system/views/setup.py) の動作設定オプションダイアログに「📐 視線許容範囲の自動調整 (キャリブレーション)」セクションを追加。
  - 「正面（アバターやカメラ）」と「判定限界（よそ見させたい境界位置）」をユーザーに順次見つめてもらい、その時の視線データを取得。
  - 両ステップ完了後、「💾 自動設定を適用する」ボタンにより正面と限界の差分を計算（安全用の上下限幅を設定）し、`st.session_state.h_range` および `st.session_state.v_range` に自動設定・スライダーとリアルタイム診断に適用されるように構成。

---

### 💡 所感・課題
プレミアム感のあるダークテーマから、柔らかく親しみやすいライトテーマへ変更したことで、学生がリラックスして面接練習を行えるような雰囲気に仕上がりました。
文字色と背景のコントラスト比が確保されており、各入力領域やテキストの視認性も高まっています。
また、重いライブラリのインポート遅延化と同期的なカメラデバイス検出のオンデマンド化を行ったことで、アプリ起動時やページ遷移時のローディング（「Loading...」表示）が一瞬（1秒未満）になり、UXが劇的に向上しました。
さらに、F5リロード時に発生していたカメラ排他ロックとタイムアウトによる「8秒の画面フリーズ」について、グローバルインスタンスを用いた解放処理と `cv2.CAP_DSHOW` による超高速起動の適用を実装したことで、Windows特有のカメラ制御の遅延・競合バグが完全に解決されました。
今回実装した視線キャリブレーション機能により、個人の顔の向きや座る位置、照明環境に起因する判定のブレ・あやふやさが排除され、実用的な精度での視線トラッキングが可能になりました。
今後、対話のターン数設定や難易度調整などのロジックを追加した際にも、この遅延ロード設計を引き継いで軽量な動作を保ちます。
